import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  calendar: {
    borderTopWidth: 1,
    paddingTop: 5,
    borderBottomWidth: 1,
    borderColor: "#eee",
    height: 350
  },
  container: {
    flex: 1,
    backgroundColor: "gray"
  }
});

export { styles };
